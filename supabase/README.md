# Supabase database files

These are the separate dojo database scripts successfully installed during Stage 2.
Do not rerun them against the live project unless an upgrade or recovery plan specifically requires it.
The initial administrator authorisation and verification were completed separately as version 0.2.5.
